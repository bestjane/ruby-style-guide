# 聆思端云一体化流程编排上手教程 (大模型硬件开发套件)

# 简介：什么是聆思端云一体化流程编排？

欢迎来到聆思(ListenAI)端云一体化流程编排教程！本教程（主要基于[聆思流程编排开发指南文档](https://docs2.listenai.com/x/RtdU8yrbO)）将引导您了解如何利用聆思的大模型开发套件硬件，并通过其云端的[聆思平台(LSPlatform)](https://platform.listenai.com/)进行可视化流程编排，从而定制设备行为并集成强大的云端AI服务。

**聆思平台 (LSPlatform) 与大模型开发套件**

[聆思平台 (LSPlatform)](https://platform.listenai.com/) 是一个为大模型应用提供从开发到部署全链路支持的云平台。其核心特色之一便是能够将硬件设备（例如聆思大模型开发套件，其详细使用可参考[相关指南](https://docs2.listenai.com/x/RtdU8yrbO)）与云端的可视化流程编排紧密结合。这意味着您可以直接在云端设计和管理运行在硬件上的应用程序逻辑，实现硬件操作与云端智能的联动。

**可视化流程编排：类似Node-RED的体验**

如果您熟悉像 [Node-RED](https://nodered.org/) ([Node-RED 指南](https://noderedguide.com/)也是一个很好的参考)这样的可视化的、基于流程的编程方式，那么您会发现LSPlatform的编排界面非常直观易用。它允许您通过拖拽不同的功能节点（Node）并将它们连接起来，从而构建出复杂的应用程序逻辑。每一个节点代表一个特定的功能，例如设备输入、数据处理、调用云端AI服务、设备输出等。这种方式极大地简化了嵌入式应用和AI应用的开发复杂度。

**核心目标：定制设备行为与集成云端AI**

本教程的核心目标是帮助开发者掌握如何通过LSPlatform的端云一体化流程编排能力，实现以下功能：

*   **定制设备行为：** 根据具体需求，灵活定义开发套件（或其他聆思硬件）的响应方式、操作逻辑和交互流程。
*   **集成云端AI服务：** 无缝接入和利用云端提供的大语言模型（LLM）、知识库问答、语音合成（TTS）等多种AI能力，为您的硬件应用赋予强大的人工智能。

通过这种编排方式，事件可以从设备端触发，流经云端进行AI处理和逻辑判断，再将结果返回到设备端执行相应的动作，形成一个完整的“端-云-端”智能应用闭环。

**目标读者**

本教程主要面向以下开发者：

*   正在使用或计划使用聆思大模型开发套件（其主要指南参考：[https://docs2.listenai.com/x/RtdU8yrbO](https://docs2.listenai.com/x/RtdU8yrbO)）或类似聆思硬件产品的开发者。
*   希望利用云端可视化流程编排工具（如[LSPlatform](https://platform.listenai.com/)）来简化和加速其硬件智能化应用开发的开发者。
*   对实现设备与云端AI服务（如大模型、知识库、TTS等）协同工作感兴趣的开发者。

接下来，让我们开始探索聆思端云一体化流程编排的强大功能吧！

## 核心概念：节点、流程与消息 (Node-RED相似概念解析)

聆思平台的端云一体化流程编排借鉴了成熟的可视化编程理念，特别是与Node-RED等工具（可参考其[官方用户指南](https://nodered.org/docs/user-guide/)或如[Node-RED编程指南的入门介绍](https://noderedguide.com/node-red-lecture-1-a-brief-introduction-to-node-red/)）在核心概念上具有相似性。理解这些基本构建模块对于高效使用平台至关重要。

### 1. 节点 (Nodes)

**在聆思平台中：**
“节点”是构成您的应用程序的基本功能单元。每一个节点都代表了一个特定的操作或处理步骤。在聆思平台的“在线编排”界面中，您可以从“节点栏”中拖拽各种预定义的节点到“编排区”。这些节点可以包括：

*   **设备交互节点：** 例如，代表“大模型开发套件”上的按键输入、传感器数据读取、屏幕显示指令、音频播放控制等。
*   **触发器节点：** 用于启动流程的特定事件，如定时触发、接收到特定网络请求等。
*   **AI服务调用节点：** 用于调用云端的AI能力，例如“星火大模型”节点（用于大语言模型推理）、“知识库检索”节点（在您的私有知识库中查找答案）、“语音合成TTS”节点（将文本转换为语音）等。
*   **逻辑运算节点：** 执行条件判断、数据转换、格式化等操作。
*   **输出节点：** 将处理结果发送到特定目标，如设备端执行某个动作，或通过网络发送数据。

**与Node-RED的相似性：**
这与Node-RED中的“节点”概念非常相似。在Node-RED中，每个节点也是一个黑盒子，它接收数据，执行特定功能，然后传递数据（更多关于Node-RED的核心概念可以参见[其文档](https://nodered.org/docs/user-guide/concepts/)）。聆思平台的节点同样封装了复杂的功能，让开发者可以专注于业务逻辑而非底层实现。

### 2. 流程 / 连接线 (Flows / Wires)

**在聆思平台中：**
当您将多个“节点”放置在“编排区”后，您需要通过“连接线”将它们连接起来，从而定义操作的顺序和数据的流向。这些连接起来的节点序列就构成了一个“流程”。

*   **定义顺序：** 连接线通常从一个节点的输出端口连接到另一个节点的输入端口，明确指示了哪个节点先执行，哪个节点后执行。
*   **数据流向：** 数据（即“消息”，见下文）会沿着这些连接线从一个节点传递到下一个节点。

**与Node-RED的相似性：**
这完全对应Node-RED中的“连线 (wires)”。通过在节点之间绘制连线，您可以构建出一个可视化的“流 (flow)”，清晰地展示了应用程序的逻辑路径。聆思平台的“编排区”就是您构建这些流程的工作台。

### 3. 消息 (Messages)

**在聆思平台中：**
“消息”是在连接的节点之间传递的数据包。当一个节点完成其操作后，它通常会将其结果或某些数据封装成一个“消息”，然后通过其输出端口发送给流程中的下一个节点。

*   **数据载体：** 消息可以包含各种类型的数据，例如：
    *   用户通过开发套件输入的语音转换后的文本。
    *   设备端摄像头采集的图像数据。
    *   大语言模型返回的文本结果。
    *   需要通过TTS节点合成为语音的文本字符串。
    *   来自传感器（如温度、湿度）的读数。
*   **结构化数据：** 消息通常是结构化的数据对象（例如JSON格式），允许节点方便地读取和修改其中的信息。例如，一个消息对象可能包含`payload`字段用于存放主要数据，还可能包含其他如`topic`等元数据字段。

**与Node-RED的相似性：**
“消息 (message)”是Node-RED的核心概念（详细解释可参考[Node-RED关于消息的文档](https://nodered.org/docs/user-guide/messages/)），通常以`msg`对象的形式存在，其中`msg.payload`是最常用的部分。聆思平台中的消息传递机制与此类似，确保了数据在不同功能节点间的顺畅流动和处理。

### 事件驱动特性

这类基于流程的编排系统（无论是聆思平台还是Node-RED）本质上是**事件驱动**的。这意味着整个流程的启动通常是由某个初始事件触发的。这个事件可能是一个设备上的物理按键被按下，一个传感器检测到了特定条件，一个HTTP请求到达了某个网络输入节点，或者一个定时器到达了预设时间。一旦流程被触发，消息就会开始在连接的节点间流动，依次执行各个节点定义的功能。

理解了节点、流程和消息这些核心概念后，您就能更好地设计和实现复杂的端云一体化应用了。在接下来的章节中，我们将通过具体示例来演示如何在聆思平台上运用这些概念。

## 前提准备与环境设置

在开始本端云一体化流程编排教程之前，请确保您已完成以下准备工作。这些是顺利进行后续步骤的基础。

### 1. 硬件准备 (Hardware)

*   **聆思大模型开发套件 (ListenAI Big Model Development Kit):**
    您需要拥有一台聆思大模型开发套件 (CSK6-MIX)。本教程中的所有操作都将围绕此硬件平台展开，其在端云一体化应用中的配置和接入主要参考[《接入云端可编排应用》](https://docs2.listenai.com/x/RtdU8yrbO)文档（本文档也是本教程后续许多步骤的原始依据）。请确保您的开发套件能够正常启动和运行。
    *   根据《[视觉语音大模型 AI 开发套件使用说明](https://docs2.listenai.com/x/nTn9kMMCU)》中的指引（具体硬件准备细节可参考其链接到的[《语音交互+识图 功能体验》文档中的硬件准备部分](https://docs2.listenai.com/x/2V18-j2v2#硬件准备)），确保至少连接任一USB口进行供电，并建议将摄像头妥善安装。

### 2. 软件/固件 (Software/Firmware)

*   **运行“语音交互+识图”应用:**
    您的开发套件应预装或已烧录并运行“语音交互+识图” (Voice Interaction + Image Recognition) 应用程序。这是我们进行端云一体化编排的基础应用。
    *   通常，开发套件出厂时默认的TF卡中已包含此应用。您可以直接在应用列表中找到并加载它。
    *   如果您的TF卡中没有此应用，或者您烧录了其他固件，请参照[《语音交互+识图 功能体验》](https://docs2.listenai.com/x/2V18-j2v2)文档中的说明（特别是“[2] 示例简介](https://docs2.listenai.com/x/2V18-j2v2#示例简介)”和可能的“[固件下载与烧录](https://docs2.listenai.com/x/UzjbjIAxw)”部分）来获取和运行此应用。
    *   成功运行后，设备界面应如《语音交互+识图 功能体验》文档中“[6] 硬件准备](https://docs2.listenai.com/x/2V18-j2v2#硬件准备)”部分所示的待机界面。

### 3. 网络配置 (Network Configuration)

*   **开发套件联网:**
    为了实现端云一体化编排和与云端AI服务的通信，您的开发套件必须成功连接到互联网（支持2.4G Wi-Fi）。
    *   请严格按照[《语音交互+识图 功能体验》文档中详细的“配置网络”步骤](https://docs2.listenai.com/x/2V18-j2v2#配置网络)来为您的设备配置Wi-Fi网络。该部分提供了UI配网和二维码配网两种方式。
    *   成功联网后，设备待机界面的Wi-Fi图标应显示正常，并且时间会更新为北京时间。

### 4. 聆思平台账户 (LSPlatform Account)

*   **注册并登录聆思平台:**
    您需要在[聆思云平台 (LSPlatform)](https://platform.listenai.com/)上拥有一个有效账户。后续的流程编排、应用管理、API调用等都将在该平台上进行。
    *   如果这是您首次使用聆思平台，您需要先访问[平台地址进行注册](https://platform.listenai.com/)。关于账户注册和获取API密钥（Request Token）的详细步骤，可以参考[《如何获取API密钥》](https://docs2.listenai.com/x/fZw6AJhn-)指南，或者我们先前编写的《聆思平台在线流程编排上手教程 (从0到1)》中的对应部分（即 `ListenAI_Orchestration_Tutorial_Complete.md` 文件中的第二部分 "Getting Started: Account and Setup"）。
    *   确保您能够成功登录 [聆思平台 (LSPlatform)](https://platform.listenai.com/)。

完成以上所有准备工作后，您就可以开始学习如何在聆思平台上进行端云一体化的流程编排了。

## 第四章：创建并理解您的第一个端云一体化应用

在完成所有前提准备后，我们现在可以开始在聆思平台 (LSPlatform) 上创建我们的第一个端云一体化应用了。我们将基于平台提供的模板来快速搭建一个应用，并理解其默认的编排流程。

### 4.1. 基于模板创建应用

为了快速开始我们的端云一体化流程编排，[聆思平台 (LSPlatform)](https://platform.listenai.com/) 提供了预设的应用模板。我们将使用“大模型套件语音交互与识图模板”，它为我们的开发套件预置了常用的功能节点和基础流程。关于如何基于此模板创建新应用，可以详细参考官方文档中[“创建新应用”](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)的章节。

请按照以下步骤创建您的应用：

1.  **访问聆思平台 (LSPlatform) 并进入应用模板中心:**
    *   打开您的网页浏览器，访问 [聆思平台 (LSPlatform)](https://platform.listenai.com/) 并登录您的账户。
    *   在平台主界面，找到并点击进入“应用模板中心”。(具体步骤可参考上述[“创建新应用”](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)文档中的截图指引)

2.  **选择应用模板:**
    *   在“应用模板中心”页面，寻找到“定制开发”标签页。
    *   在“定制开发”标签下，找到名为“大模型套件语音交互与识图模板”的模板。
    *   点击该模板下方的“添加应用”按钮。
        *(聆思文档[“创建新应用”](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)章节此处会有一张图片，展示了在应用模板中心选择“大模型套件语音交互与识图模板”并点击“添加应用”按钮的界面。请参考原始文档截图，标记为：“选择模板并添加应用示意图”)*

3.  **命名并创建应用:**
    *   点击“添加应用”后，会弹出一个窗口要求您为新应用命名。
    *   输入一个具有描述性的应用名称（例如：“我的第一个端云应用” 或 “My_Device_Cloud_App”）。
    *   输入名称后，点击“确认创建应用”按钮（或类似确认按钮）。

完成以上步骤后，您的新应用程序就会基于所选模板成功创建。接下来，我们就可以进入这个应用的可视化编排界面，查看和修改其预设的流程了。

### 4.2. 理解默认编排流程

创建完应用后，下一步是进入其编排界面，熟悉模板预设的流程和节点。这将帮助我们理解端云一体化应用是如何通过可视化方式构建和运作的。

1.  **打开新创建的应用并进入编排界面:**
    *   返回到聆思平台 (LSPlatform) 的应用列表或应用管理界面。
    *   找到您在上一节 (4.1) 中创建的应用，并点击它。
    *   在应用详情页面，点击“编排应用”按钮。这一步骤在聆思官方的[“创建新应用”文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)中也有图示说明（通常在模板创建成功后，下一步即是进入编排界面）。
        *(聆思文档此处会有一张图片，展示了应用详情页以及“编排应用”按钮的位置。请参考原始文档截图，标记为：“进入应用编排界面示意图”)*

2.  **探索预设的编排流程:**
    点击“编排应用”后，您将进入可视化应用编排界面。您会看到一个已经由许多节点和连接线组成的流程图。这就是“大模型套件语音交互与识图模板”预设的端云一体化业务流程。您可以对照[“创建新应用”文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)中展示的示例工程节点图和业务流程图来理解这个默认编排。
    *(聆思文档[“创建新应用”](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)章节此处会有一张图片，展示了模板应用默认的节点编排图。请参考原始文档截图，标记为：“模板应用默认编排流程图”)*

    根据[该文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)的描述，这个预设的示例工程实现了一个相当智能的多模态交互流程，主要包括以下功能：
    *   **图生文 (Image-to-Text):** 理解用户拍摄的图片内容。
    *   **文生图 (Text-to-Image):** 根据用户语音描述生成图片。
    *   **知识库问答 (Knowledge Base Q&A):** 基于您提供的知识库回答特定领域问题。
    *   **兜底闲聊 (Fallback Chat):** 在无法匹配特定意图或知识库内容时，进行通用聊天。

    **流程逻辑解析 (依据[官方文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)流程解析部分)：**
    该模板的业务流程大致如下：
    1.  **优先处理连续对话中的图生文：** 系统首先会检查最近（例如3轮内）的对话历史，判断用户是否正在进行一个关于图片的连续对话（例如，用户上传了一张图，然后继续追问关于这张图的问题）。如果是，则继续调用大模型进行图片理解，并将结果通过流式TTS（语音合成）和流式文本下发给设备。
    2.  **意图识别（是否“画画”）：** 如果不是连续的图生文场景，系统会利用大模型的意图识别能力，判断用户的输入是否包含“画画”或类似的意图。
    3.  **执行文生图：** 如果用户的意图被识别为“画画”，流程将转向文生图逻辑。这包括调用相应的AI模型根据用户的文本描述生成图片，并将生成的图片显示在设备屏幕上，同时可能伴有TTS语音反馈。
    4.  **知识库检索：** 如果用户的意图不是“画画”，系统会尝试在预设的知识库中检索与用户提问相关的内容。如果找到了匹配的知识点，则调用大模型围绕这些知识点组织答案，并通过流式TTS和文本下发。
    5.  **兜底闲聊：** 如果用户的意图既不是“画画”，在知识库中也没有找到相关答案，那么流程会进入兜底闲聊模式。系统会调用大模型进行通用的、开放式的对话回复，并通过流式TTS和文本下发。

3.  **关键节点解读 (初探):**
    在流程图中，您会看到多种不同功能的节点。虽然我们将在后续章节详细介绍如何配置它们，但这里可以初步认识几个核心的聆思平台特有节点（具体名称和图标请参照您界面中的实际显示，也可对照[官方文档中的流程图](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)进行识别）：
    *   **图片理解/视觉模型节点:** 用于处理和分析从设备端上传的图像数据，实现“图生文”功能。
    *   **文本生成/大语言模型节点:** 核心的LLM调用节点，用于意图识别、组织回答、生成对话内容以及“文生图”中的文本处理。
    *   **知识点检索节点:** 连接到您在LSPlatform上创建的知识库，根据输入文本检索相关知识条目。
    *   **TTS流式合成节点:** 将文本（例如大模型的回复、知识库答案）实时合成为语音流，并发送到设备端播放。
    *   **设备输入/输出节点:** 代表与大模型开发套件硬件的交互，例如接收语音指令、获取摄像头图像、在屏幕上显示文本或图片、播放音频等。

通过对这个默认流程的初步探索，您可以大致了解一个端云一体化应用是如何通过不同功能节点的组合与连接，来实现复杂的多模态AI交互逻辑的。在接下来的步骤中，我们将学习如何修改和定制这个流程。

## 第五章：关联云端应用与硬件设备

完成了云端应用的初步创建和流程探索后，我们需要将这个云端应用与我们的物理硬件——聆思大模型开发套件——关联起来。在[聆思平台 (LSPlatform)](https://platform.listenai.com/)中，这个关联是通过“产品 (Product)”来实现的。“产品”可以看作是连接您的云端应用逻辑和实际硬件设备的桥梁和容器。

### 5.1. 在LSPlatform创建“产品”

首先，我们需要在LSPlatform上创建一个新的“产品”。详细的图文指引可以参考官方文档中关于[“新建产品”](https://docs2.listenai.com/x/RtdU8yrbO#h-1-新建产品)的说明。

1.  **导航至“产品管理”模块:**
    *   登录到您的 [聆思平台 (LSPlatform)](https://platform.listenai.com/)。
    *   在平台的左侧导航栏或主菜单中，找到并点击“产品管理”模块。

2.  **新建产品:**
    *   在“产品管理”界面中，点击“新建产品”按钮。具体操作界面可参照上述[“新建产品”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-1-新建产品)中的截图。
        *(聆思文档此处通常会有一张图片，展示了“产品管理”界面以及“新建产品”按钮的位置。请参考原始文档截图，标记为：“产品管理界面与新建产品按钮示意图”)*

3.  **填写产品信息:**
    *   点击“新建产品”后，平台会要求您填写新产品的相关信息。
    *   根据界面提示（同样可参考[“新建产品”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-1-新建产品)中的图示），最重要的通常是为您的产品设置一个明确的“产品名称”。请按照平台的指引填写此名称（例如，您可以命名为：“我的大模型套件产品”或“My_DevKit_Product”）。
        *(聆思文档此处通常会有一张图片，展示了填写产品名称的对话框或表单。请参考原始文档截图，标记为：“填写产品名称示意图”)*
    *   填写完毕后，点击确认或保存按钮以创建产品。

创建“产品”是在LSPlatform上管理和配置设备接入、应用绑定以及后续设备认证等步骤的基础。完成这一步后，我们接下来需要将之前创建的云端应用配置到这个新产品中。

### 5.2. 为“产品”配置应用

创建了“产品”之后，它目前还是一个空的容器。我们需要将之前在第四章中基于模板创建并初探过的那个云端编排应用（即“大模型套件语音交互与识图模板”的实例）指定给这个“产品”。这样，平台才知道当设备连接到这个“产品”时，应该执行哪个云端应用的逻辑。详细的图文指引可以参考官方文档中关于[“配置应用”](https://docs2.listenai.com/x/RtdU8yrbO#h-2-配置应用)到产品的说明。

请按照以下步骤为您的“产品”配置应用：

1.  **进入产品配置界面:**
    *   在“产品管理”模块中，找到您在上一节 (5.1) 创建的“产品”。
    *   点击该“产品”进入其详情或配置页面。

2.  **配置大模型应用:**
    *   在产品的配置页面中，查找与“配置应用”、“应用选择”或类似字样相关的设置区域 (具体界面可参考上述[“配置应用”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-2-配置应用)中的截图)。
    *   您会看到一个列表或选项，允许您选择要关联到此产品的云端应用。
    *   根据聆思官方文档的指引（“在此我们仅需在配置应用勾对第一阶段已经创建的大模型应用即可。”），找到您在第四章第一节中创建的那个基于“大模型套件语音交互与识图模板”的应用。
    *   勾选或选择这个应用，以将其与当前“产品”绑定。
        *(聆思文档[“配置应用”](https://docs2.listenai.com/x/RtdU8yrbO#h-2-配置应用)章节此处会有一张图片，展示了在产品配置中勾选或选择对应大模型应用的界面。请参考原始文档截图，标记为：“为产品配置大模型应用示意图”)*

3.  **保存配置:**
    *   完成选择后，确保点击页面上的“保存”、“应用”或“确认”按钮，使配置生效。

通过以上步骤，您就成功地将具体的云端编排应用逻辑与您定义的“产品”关联起来了。这意味着，当您的开发套件（设备）后续通过这个“产品”的身份接入平台时，它将会执行这个特定云端应用所编排的流程。下一步，我们将配置设备本身，使其能够识别并连接到这个“产品”。

### 5.3. 获取设备ID (Chip ID)

每个聆思大模型开发套件都有一个唯一的设备ID (也称为 Chip ID)。这个ID是设备在聆思平台 (LSPlatform) 上进行身份认证和实现端云交互的关键凭证。在将设备添加到您创建的“产品”的设备白名单之前，我们必须先获取到这个ID。聆思官方文档中关于[“读取设备ID”](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)的章节详细介绍了以下几种方法。

#### 方法一：从设备设置中查看

这种方法直接在开发套件的屏幕上操作，最为直观。具体步骤可参考上述[“读取设备ID”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)中的“设置中查看”部分。

1.  **确保设备运行“语音交互+识图”应用：** 您的开发套件应已启动并正在运行“大模型语音交互+识图”这个示例应用。
2.  **进入应用信息查看页：**
    *   在应用的主界面（待机页面），从屏幕顶部向下滑动，调出下拉菜单。
    *   点击菜单中的“设置”图标（通常是一个齿轮形状的图标）。
    *   在设置菜单中，找到并点击“应用”选项。
    *   进入“应用信息查看页”后，您就可以看到当前设备的ID信息。
        *(聆思文档[“读取设备ID”](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)章节此处会有一张图片，展示了在设备屏幕上通过 设置 → 应用 查看设备ID的完整路径和最终显示ID的界面。请参考原始文档截图，标记为：“设备设置中查看ID示意图”)*

#### 方法二：通过串口指令读取

如果您习惯使用命令行或者设备屏幕操作不便，可以通过串口连接来获取设备ID。具体步骤可参考[“读取设备ID”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)中的“串口读取”部分。

1.  **连接DAP_USB接口：** 使用USB数据线将开发套件的DAP_USB接口连接到您的电脑。
2.  **打开串口终端工具：**
    *   您可以使用聆思官方提供的“[在线串口终端](https://tool.listenai.com/serial-term/)”。
    *   或者使用您电脑上安装的其他串口调试工具（如PuTTY, Tera Term等）。
    *   确保选择了正确的串口号，波特率通常默认为115200。
3.  **发送获取ID指令：**
    *   在串口终端中，输入以下指令并发送（回车）：
        ```
        get chip_id
        ```
    *   发送指令后，设备ID将会显示在终端的返回信息中。
        *(聆思文档[“读取设备ID”](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)章节此处会有一张图片，展示了通过串口终端发送 `get chip_id` 指令并成功获取到设备ID的界面。请参考原始文档截图，标记为：“串口指令获取ID示意图”)*

#### 方法三：使用 `cskburn desktop` 烧录工具读取

`cskburn desktop` 是聆思官方推出的桌面烧录工具，除了固件烧录功能外，也可以用来获取设备信息。具体步骤可参考[“读取设备ID”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)中的“cskburn读取”部分。

1.  **下载并安装工具：**
    *   如果尚未安装，请先访问[`cskburn desktop` 烧录工具的说明及下载页面](https://docs2.listenai.com/x/oo2_KzYFd)进行下载并安装。
2.  **运行工具并连接设备：**
    *   确保您的开发套件通过USB（通常是DAP_USB口）连接到电脑。
    *   双击 `cskburn desktop` 工具的图标运行软件。
3.  **获取设备信息：**
    *   在 `cskburn desktop` 工具界面中，首先从串口下拉框中选择您的开发套件连接后被电脑识别到的串口编号。
    *   然后，点击界面上的“获取信息”按钮。
    *   工具执行后，会在相应的区域显示出包括设备ID在内的设备信息。
        *(聆思文档[“读取设备ID”](https://docs2.listenai.com/x/RtdU8yrbO#h-3-读取设备id)章节此处会有一张图片，展示了 `cskburn desktop` 工具的界面，包括选择串口、点击“获取信息”按钮以及显示设备ID的区域。请参考原始文档截图，标记为：“cskburn工具获取ID示意图”)*

请选择以上任意一种方法获取您的设备ID，并妥善记录下来。在接下来的步骤中，我们会用到这个ID来完成设备在云端的注册。

### 5.4. 添加设备到白名单

获取到开发套件唯一的设备ID (Chip ID) 后，我们需要将其添加到您在聆思平台 (LSPlatform) 上创建的“产品”的设备白名单中。这是一个重要的安全和管理步骤。详细的图文指引可以参考官方文档中关于[“添加设备白名单”](https://docs2.listenai.com/x/RtdU8yrbO#h-4-添加设备白名单)的说明。

**为什么需要白名单?**

聆思平台 (LSPlatform) 出于安全考虑，只会对已通过身份认证的设备提供交互服务。将设备的ID添加到“产品”的白名单中，就相当于在该“产品”下注册了这个设备，允许它访问与该“产品”关联的云端应用和服务。未经白名单授权的设备将无法与您的云端应用进行正常的端云交互。

**如何添加设备ID到白名单:**

1.  **进入产品的“设备管理”页面:**
    *   登录到您的 [聆思平台 (LSPlatform)](https://platform.listenai.com/)。
    *   在“产品管理”模块中，找到并进入您在之前步骤中创建并配置好应用的那个“产品”。
    *   在选定产品的详情或管理界面中，查找并点击进入“设备管理”页面（或类似名称的设备列表管理入口）。(具体界面可参考上述[“添加设备白名单”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-4-添加设备白名单)中的截图)

2.  **导入/添加设备ID:**
    *   在“设备管理”页面，您通常会看到一个用于添加或导入设备ID的按钮或功能区。
    *   点击此按钮，然后将您在上一节 (5.3) 中获取到的设备ID输入或粘贴到指定的输入框中。
    *   确认无误后，提交或保存该设备ID。
        *(聆思文档[“添加设备白名单”](https://docs2.listenai.com/x/RtdU8yrbO#h-4-添加设备白名单)章节此处会有一张图片，展示了在产品的“设备管理”页面导入或添加设备ID的操作界面。请参考原始文档截图，标记为：“设备管理与添加ID到白名单示意图”)*

**重要注意事项:**

*   **免费测试ID额度:**
    根据聆思平台的说明：“平台提供10个免费测试ID用于产品验证阶段测试使用，如需新增额度，请在平台联系商务。” 请注意您账户下的可用测试ID数量。

*   **产品管理与额度限制:**
    同样根据聆思平台的提示：“产品管理暂不支持删除，如额度已满请在已创建的产品上进行体验。” 如果您的免费ID额度已用完，并且无法删除不再使用的“产品”来释放额度，您可能需要联系商务支持，或者在已有的、尚有空余设备槽位的“产品”上进行后续的测试和体验。

成功将设备ID添加到白名单后，您的开发套件在法理上就被授权可以连接到您在LSPlatform上定义的这个“产品”了。最后一步是让设备本身知道它应该连接哪个“产品”。

### 5.5. 设备端写入产品凭证

现在，云端的“产品”已经配置完毕，并且您的设备ID也已经添加到了该“产品”的白名单中。最后一步是将“产品”的身份凭证写入到您的聆思大模型开发套件中。这样，设备在启动和联网后，才知道应该连接到云端的哪个“产品”，并使用正确的密钥进行身份验证。聆思官方文档中关于[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)的章节详细介绍了以下两种方法。

这些凭证主要是指“产品ID (Product ID)”和“产品密钥 (Secret ID)”。

**在哪里找到产品凭证?**

您可以在 [聆思平台 (LSPlatform)](https://platform.listenai.com/) 上，进入您所创建的“产品”的“产品信息”或“概览”页面找到对应的 `product_id` 和 `secret_id` (有时也称作 Product Secret)。

聆思官方文档提供了以下两种将这些凭证写入设备的方法：

#### 方法一：扫描二维码接入

这种方法利用开发套件的摄像头扫描在LSPlatform上为“产品”生成的特定二维码，自动完成配置。具体步骤可参考上述[“设备端写入配置”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)中的“扫描二维码接入”部分。

**注意：** 此方法要求您设备上当前运行的固件支持“扫码接入”功能。如果您的固件版本较旧或不包含此功能，文档建议参照“[《应用合集TF卡》](https://docs2.listenai.com/x/oEuqR5JaN)更新 TF 卡中的镜像文件夹”来获取最新固件。

**操作步骤：**

1.  **在设备上进入扫码界面：**
    *   确保设备运行“语音交互+识图”应用。
    *   在应用的主界面（待机页面），从屏幕顶部向下滑动，调出下拉菜单。
    *   依次点击“设置”图标 → “应用”，进入应用信息查看页。
        *(聆思文档[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)章节此处会有一张图片，展示了在设备屏幕上通过 设置 → 应用 进入应用信息查看页的路径。请参考原始文档截图，标记为：“设备端进入应用信息页示意图”)*
    *   在应用信息查看页，点击右下角的“扫码接入”按钮，设备会调出相机并进入扫描模式。
        *(聆思文档[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)章节此处会有一张图片，展示了应用信息查看页以及右下角的“扫码接入”按钮。请参考原始文档截图，标记为：“设备端扫码接入按钮示意图”)*

2.  **扫描云端产品二维码：**
    *   在LSPlatform上，找到您“产品”对应的接入二维码（通常在产品详情或设备接入指引部分）。
    *   将设备摄像头对准电脑屏幕上显示的LSPlatform产品二维码，确保二维码完整清晰地位于设备屏幕的绿色扫描框内。
        *(聆思文档[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)章节此处会有一张图片，展示了设备摄像头扫描LSPlatform上产品二维码的场景。请参考原始文档截图，标记为：“设备扫描产品二维码示意图”)*

3.  **验证配置信息：**
    *   扫描成功后，设备会自动解析二维码中的信息。
    *   请在设备的应用配置信息页面（通常是扫描成功后自动返回或可以手动查看的页面），检查 `product_id` 和 `secret_id` 是否已经更新为您的目标“产品”的正确信息。

#### 方法二：通过命令行接入 (CLI)

如果您偏好使用命令行，或者扫码功能因故无法使用，可以通过串口命令行来手动配置这些ID。具体步骤可参考[“设备端写入配置”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)中的“命令行接入”部分。

**操作步骤：**

1.  **连接设备并打开串口终端：**
    *   使用USB数据线连接开发板的DAP_USB接口到您的电脑。
    *   打开[聆思在线串口终端工具](https://tool.listenai.com/serial-term/)或您本地的串口调试工具。
    *   选择正确的串口设备进行连接，波特率默认通常为115200。点击连接按钮。
        *(聆思文档[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)章节此处会有一张图片，展示了在线串口终端工具的连接设置界面。请参考原始文档截图，标记为：“串口终端连接设置示意图”)*

2.  **执行配置指令：**
    *   在串口终端中，分别使用以下指令来配置产品ID和产品密钥。请将指令中的占位符替换为您从LSPlatform上获取到的实际ID和密钥：
        ```
        set product_id <替换为您的产品ID>
        set secret_id <替换为您的产品密钥>
        ```
        例如：
        ```
        set product_id 71cd1721-xxx-xxx-xxx-1000055c8b2f
        set secret_id cb09013f-xxx-xxx-xxx-e212b32f9218
        ```
    *   **旧版固件注意：** 文档特别提到，“旧版固件请使用 `aiui set product_id` 与 `aiui set secret_id` 指令。” 如果上述 `set` 指令无效，请尝试使用 `aiui set` 前缀的指令。
        *(聆思文档[“设备端写入配置”](https://docs2.listenai.com/x/RtdU8yrbO#h-5-设备端写入配置)章节此处会有一张图片，展示了在串口终端中成功执行 `set product_id` 和 `set secret_id` 指令的示例。请参考原始文档截图，标记为：“串口指令配置产品凭证示意图”)*

完成产品凭证的写入后，文档建议**重启开发板**（例如通过按动RST按键或拨动电源开关），使设备重新加载并运行应用。此时，您的开发套件应该会自动尝试连接到您在LSPlatform上配置的新“产品”，并使用新的凭证进行认证。如果一切顺利，设备将成功接入云端，并准备好执行您编排的流程了。

## 第六章：自定义端云一体化流程

在前面的章节中，我们成功地基于模板创建了一个云端应用，将其与“产品”关联，并将开发套件接入到了该“产品”。我们所使用的“大模型套件语音交互与识图模板”本身已经提供了一套相当完善的多模态交互流程。然而，在实际应用开发中，我们往往需要根据具体的业务需求或特定的垂直领域场景来调整和扩展这个默认流程。

### 6.1. 自定义流程简介

**为什么要自定义流程？**

默认的应用模板为我们提供了一个强大的起点，但“一刀切”的解决方案很难满足所有个性化的需求。以下是一些常见的自定义流程的理由：

*   **添加新功能：** 您可能希望集成模板中未包含的特定AI能力（例如，特定场景的图像分析、调用第三方API获取实时数据、控制其他智能家居设备等）。
*   **改变现有逻辑：** 您可能需要调整默认流程的判断条件、分支走向或数据处理方式，以更贴合您的业务规则。例如，修改意图识别后的处理逻辑，或者调整不同AI服务调用的优先级。
*   **集成自有数据或API：** 将您自己的业务数据、私有知识库、或内部API服务接入到流程中，使AI应用能够利用这些特有资源。
*   **针对垂直领域进行优化：** 不同的行业（如教育、医疗、客服、工业等）对AI交互的侧重点和所需知识大相径庭。通过自定义流程，可以深度优化应用在特定垂直领域的表现。
*   **简化或裁剪功能：** 如果默认模板中的某些功能对您的应用场景是冗余的，您可以将其从流程中移除，以提高效率或降低成本。

**LSPlatform流程编排的灵活性 (类似Node-RED)**

如果您熟悉[Node-RED](https://nodered.org/)（一个流行的开源流程编程工具，其[入门介绍](https://noderedguide.com/node-red-lecture-1-a-brief-introduction-to-node-red/)可以帮助您了解其基本理念），那么您会发现聆思平台 (LSPlatform) 的可视化流程编排在灵活性方面与之非常相似。平台的设计理念就是让开发者能够轻松地修改和创建复杂的事件驱动型应用，而无需编写大量底层代码。

*   **拖拽式操作：** 您可以从节点库中拖拽新的功能节点到编排画布上。
*   **节点配置：** 每个节点通常都提供了丰富的配置选项，允许您调整其行为参数（例如，为大模型节点选择不同的模型版本、温度参数，为知识库节点指定要检索的知识库等）。
*   **连线定义逻辑：** 通过删除、添加或修改节点之间的连接线，您可以直观地改变数据的流向和业务逻辑的执行顺序。

这种高度的可视化和模块化特性，使得即使是复杂的端云一体化应用逻辑，也能够以相对简单和直观的方式进行定制和迭代。

**本章目标：通过实例学习自定义**

为了具体展示如何自定义流程，在接下来的小节中，我们将以一个常见的需求为例——**为应用集成一个自定义知识库，以实现更专业的领域问答能力**。这个示例的灵感和主要步骤来源于聆思官方文档中的[“示例-增加知识库检索”](https://docs2.listenai.com/x/RtdU8yrbO#示例-增加知识库检索)章节。我们将涉及到在默认流程的基础上，添加和配置知识库检索相关的节点，并调整原有逻辑，使其能够优先从我们的自定义知识库中寻找答案。

通过这个实例，您将掌握在LSPlatform上修改现有编排流程、添加新功能节点、以及配置节点参数的基本方法。

### 6.2. 在LSPlatform创建知识库

为了让我们的AI应用能够回答特定领域的问题（例如，关于特定产品、公司政策或专业领域知识），我们需要为其提供相应的知识。在聆思平台 (LSPlatform) 上，这是通过“知识库”功能来实现的。本节将指导您如何创建一个新的知识库并上传相关文档。整个过程——包括创建知识库、上传文档及设置分片策略——在聆思官方的[“示例-增加知识库检索”](https://docs2.listenai.com/x/RtdU8yrbO#示例-增加知识库检索)文档中均有详细图文说明。

**重要提示：首次使用知识库的API密钥**

根据聆思官方文档的说明：“**第一次使用知识库需要创建密钥，请在[密钥页面](https://platform.listenai.com/keys)进行创建对应的API KEY。**” 如果您之前没有在LSPlatform上使用过知识库相关的API或服务，请务必先访问[密钥管理页面](https://platform.listenai.com/keys)（关于API密钥的获取和管理，也可参考通用指南[《如何获取API密钥》](https://docs2.listenai.com/x/fZw6AJhn-)），确保为知识库服务创建并启用了相应的API Key。否则，后续的知识库操作（如检索）可能会失败。

**创建知识库并上传文档的步骤：**

1.  **导航至“知识库”模块:**
    *   登录到您的 [聆思平台 (LSPlatform)](https://platform.listenai.com/)。
    *   在平台的左侧导航栏（侧边栏）中，找到并点击“知识库”模块。这将带您进入知识库管理页面。

2.  **创建新知识库:** (参考：[官方文档-新建知识库](https://docs2.listenai.com/x/RtdU8yrbO#新建知识库))
    *   在知识库管理页面，点击“创建知识库”按钮。
        *(聆思文档[“新建知识库”](https://docs2.listenai.com/x/RtdU8yrbO#新建知识库)章节此处会有一张图片，展示了知识库管理页面以及“创建知识库”按钮的位置。请参考原始文档截图，标记为：“知识库管理与创建按钮示意图”)*
    *   在弹出的窗口中，输入您的知识库名称（例如：“我的产品手册知识库”或“空调使用指南”）。
    *   输入名称后，点击“确定”按钮完成知识库的创建。
        *(聆思文档[“新建知识库”](https://docs2.listenai.com/x/RtdU8yrbO#新建知识库)章节此处会有一张图片，展示了输入知识库名称的对话框。请参考原始文档截图，标记为：“输入知识库名称示意图”)*

3.  **进入知识库并上传文件:** (参考：[官方文档-上传文档](https://docs2.listenai.com/x/RtdU8yrbO#上传文档))
    *   知识库创建成功后，它会出现在您的知识库列表中。点击您刚刚创建的那个知识库的名称，进入其详情管理界面。
    *   在知识库详情界面，找到并点击右侧的“上传文件”按钮，准备向知识库中添加文档内容。
        *(聆思文档[“上传文档”](https://docs2.listenai.com/x/RtdU8yrbO#上传文档)章节此处会有一张图片，展示了特定知识库的管理界面以及“上传文件”按钮。请参考原始文档截图，标记为：“知识库详情与上传文件按钮示意图”)*

4.  **选择并上传文档:**
    *   点击“上传文件”后，系统会提示您选择要上传的文档。
    *   **支持的文档格式：** 聆思平台目前支持 `txt`、`doc` (Microsoft Word文档)、`pdf` 格式的文档。
    *   **格式转换提示：** 如果您的知识内容存储在其他格式的文件中（例如 Markdown、Excel、HTML等），请务必先将其内容转换并保存为上述支持的格式之一，然后再进行上传。
    *   选择您准备好的文档文件，然后确认上传。
        *(聆思文档[“上传文档”](https://docs2.listenai.com/x/RtdU8yrbO#上传文档)章节此处会有一张图片，展示了文件选择和上传的界面。请参考原始文档截图，标记为：“选择并上传文档示意图”)*

5.  **设置分片策略:** (参考：[官方文档-设置分片策略](https://docs2.listenai.com/x/RtdU8yrbO#设置分片策略))
    *   文档上传后，平台需要对文档内容进行“分片”(Chunking)，即将长文档切分成较小的片段，以便于后续的检索和处理。系统会要求您选择分片策略。
    *   聆思平台提供了“智能分片”等选项。对于大多数情况，您可以选择默认的“智能分片”策略。
    *   选择好分片策略后，点击“保存”按钮。
        *(聆思文档[“设置分片策略”](https://docs2.listenai.com/x/RtdU8yrbO#设置分片策略)章节此处会有一张图片，展示了选择分片策略的界面，例如高亮显示“智能分片”选项。请参考原始文档截图，标记为：“设置分片策略示意图”)*

完成以上步骤后，您选择的文档内容就会被处理并存储到您的知识库中。平台会对这些内容建立索引，以便后续能够通过AI应用流程中的“知识点检索”节点进行高效查询。

现在，您已经拥有了一个包含自定义知识的知识库。下一步，我们将学习如何修改云端应用的编排流程，将这个新创建的知识库集成进去。

### 6.3. 修改编排流程以集成知识库

创建并填充好自定义知识库后，我们需要告诉我们的云端应用流程在何时以及如何使用这个知识库。这通常涉及到修改应用中预设的“知识点检索”节点，将其指向我们新创建的知识库。聆思官方文档中关于此操作的详细图文指引位于[“在线编排”](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)章节。

**操作步骤：**

1.  **打开目标应用并进入编排界面:**
    *   登录到您的 [聆思平台 (LSPlatform)](https://platform.listenai.com/)。
    *   导航至“应用管理”或您的应用列表。
    *   找到并打开您在第四章第一节 (4.1) 中基于“大模型套件语音交互与识图模板”创建的那个应用实例。**请确保这是已经与您的“产品”以及物理设备关联的那个应用。**
    *   在应用详情页面，点击“编排应用”按钮，进入该应用的可视化流程编排界面。
        *(聆思文档[“在线编排”](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)章节此处通常会有一张图片，展示了在线编排的整体界面，可能高亮显示了某些关键区域。请参考原始文档截图，标记为：“应用编排界面概览图”)*

2.  **定位“知识点检索”节点:**
    *   在应用编排的画布上，您会看到由多个节点和连接线组成的流程图。这是模板预设的逻辑。
    *   仔细查找流程中名为“知识点检索”的节点（或功能相似的节点，具体名称以您界面为准）。根据模板的逻辑（如4.2节所述），这个节点的作用是在用户提问后，尝试从知识库中寻找答案。
        *(聆思文档[“在线编排”](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)章节此处会有一张图片，在复杂的流程图中高亮标出“知识点检索”节点的位置。请参考原始文档截图，标记为：“定位知识点检索节点示意图”)*

3.  **配置“知识点检索”节点:** (具体操作可对照上述[“在线编排”文档链接](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)中的图示)
    *   **打开配置界面：** 选中“知识点检索”节点。通常，您可以通过双击该节点，或者单击选中后在右侧的“辅助栏”中找到其配置选项，来打开该节点的配置界面。
    *   **选择知识库：** 在“知识点检索”节点的配置界面中，您会看到一个用于选择知识库的选项，这通常是一个下拉菜单或列表。
    *   点击该下拉菜单或选择器，从列出的知识库中找到并选择您在上一节 (6.2) 中创建的那个自定义知识库（例如，您命名的“我的产品手册知识库”或“空调使用指南”）。
        *(聆思文档[“在线编排”](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)章节此处会有一张图片，清晰展示了“知识点检索”节点的配置面板，特别是高亮了选择知识库的下拉菜单。请参考原始文档截图，标记为：“配置知识点检索节点并选择知识库示意图”)*
    *   **保存配置：** 选择完毕后，确保在该配置界面点击“确定”、“保存”或应用更改的按钮。

通过以上步骤，您就成功地将云端应用流程中的“知识点检索”功能指向了您自定义的知识库。这意味着，当流程执行到这一步时，它会尝试从您上传的文档内容中查找与用户问题相关的答案。

在下一节，我们将把修改后的应用部署到生产环境，并通过开发套件实际测试知识库问答的效果。

### 6.4. 部署变更

在上一节中，我们成功修改了云端应用的编排流程，将“知识点检索”节点指向了我们自定义的知识库。现在，为了让这些更改生效，并最终体现在与我们物理设备（聆思大模型开发套件）的交互中，我们需要将修改后的应用部署到相应的运行环境。整个部署流程——包括在编排界面部署、返回主界面部署生产以及重启设备——在聆思官方文档的[“在线编排”章节末尾](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)有明确的操作指引和图示。

聆思平台 (LSPlatform) 通常提供至少两级部署：

1.  **部署到编排环境（测试/预备环境）：**
    *   当您在“在线编排”界面完成了对流程的修改（例如，配置了“知识点检索”节点指向新的知识库）之后，首先需要将这些改动部署到与编排界面关联的测试或预备环境中。
    *   **操作：** 在“在线编排”页面的右上角，找到并点击“部署”按钮。
        *(聆思文档[“在线编排”章节](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)此处会有一张图片，展示了在线编排界面右上角的“部署”按钮。请参考原始文档截图，标记为：“编排界面部署按钮示意图”)*
    *   此操作会将您当前的流程保存并部署。

2.  **部署到生产环境：**
    *   仅仅在编排界面点击“部署”通常只是将更改应用到了一个测试或开发环境。为了让连接到您“产品”的实际物理设备能够使用这个更新后的流程，您需要将其进一步“部署生产”。
    *   **操作：**
        *   完成在编排界面的“部署”后，返回到聆思平台 (LSPlatform) 的主应用界面或应用列表页面。
        *   找到您刚刚修改并部署过的那个应用。
        *   点击该应用对应的“部署生产”按钮。
            *(聆思文档[“在线编排”章节](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)此处会有一张图片，展示了在应用列表或应用详情页的“部署生产”按钮。请参考原始文档截图，标记为：“应用列表部署生产按钮示意图”)*
    *   这将把包含最新修改（集成了新知识库）的应用版本推送到正式的生产运行环境。

3.  **重启设备并验证：**
    为了确保您的开发套件能够加载并运行最新的云端应用配置（包括新指向的知识库），聆思官方文档在[“在线编排”章节的末尾](https://docs2.listenai.com/x/RtdU8yrbO#在线编排)明确指示：
    **“完成上述部署后，重启设备，此时自定义应用已经完成修改。”**
    *   请按照此指引，在完成云端的“部署”和“部署生产”操作后，重新启动您的聆思大模型开发套件。

设备重启后，它将会连接到您配置的“产品”，并拉取最新的生产环境应用流程。现在，当您通过设备与AI进行交互，并且提问涉及到您自定义知识库中的内容时，应用应该能够从该知识库中检索信息并给出相应的回答了。在下一章，我们将具体测试这个自定义后的知识库问答功能。

### 6.5. 测试自定义后的流程

完成了对云端应用流程的修改和部署，并且重启了开发套件之后，最关键的一步就是进行实际测试，以验证我们的自定义功能是否按预期工作。在这个例子中，我们需要测试的是集成了自定义知识库后的问答能力。

**测试的重要性：**

每当您对应用流程进行了修改（无论是调整节点配置、改变连接逻辑，还是像本例中一样集成了新的数据源如知识库），都必须进行充分的测试。这能确保：

*   更改已正确应用。
*   新功能按预期运行。
*   没有意外引入新的问题或破坏原有功能。

**如何测试知识库集成：**

1.  **与开发套件进行语音交互：**
    *   确保您的聆思大模型开发套件已开机、联网，并且成功连接到您在LSPlatform上配置的“产品”。
    *   按照您在设备上设置的交互方式（例如，语音唤醒“小美小美”，或按键触发），启动与设备的对话。

2.  **提出针对性问题：**
    *   **关键在于，您需要向设备提出那些答案包含在您之前上传到自定义知识库的文档中的问题。**
    *   例如，如果您上传的是“空调使用说明书”，您可以问：“如何清洁空调的过滤网？”或“这款空调有哪些节能模式？”
    *   如果上传的是公司内部的“员工IT支持手册”，您可以问：“忘记了邮箱密码怎么办？”或“如何申请VPN访问权限？”

3.  **观察和评估回答：**
    *   仔细聆听设备的语音回答，并观察屏幕上可能显示的文本内容。
    *   **预期结果：** 如果一切配置正确，设备现在应该能够针对您的提问，给出基于您上传到知识库的文档内容的回答。这些回答会比之前单纯依靠大模型通用知识库的回答更为精准和专业（在您自定义知识库所覆盖的领域内）。
    *   您应该能分辨出这些回答是来源于您的特定知识库，而不是大模型的通用闲聊，也不是先前流程中可能处理的其他意图（如图生文或文生图）。

**参考示例：**

聆思官方文档在[“示例-增加知识库检索”章节的末尾](https://docs2.listenai.com/x/RtdU8yrbO#示例-增加知识库检索)提供了一张交互效果图（图片名为 `73074f126cdb634bc4817e5dcd077b1c.jpg`），展示了在集成了特定知识库（例如关于空调的知识）后，设备能够正确回答相关问题的场景。您可以参考这张图片来理解测试成功时设备大致的交互表现。
*(聆思文档此处通常会有一张图片，展示了设备在集成了特定知识库后，用户提问相关问题，设备给出基于该知识库回答的交互截图。请参考原始文档中的该图片。)*

如果设备能够正确回答您基于自定义知识库提出的问题，那么恭喜您！您已成功完成了对端云一体化流程的自定义，并成功集成了领域知识库。如果回答不符合预期，您可能需要回顾之前的步骤，检查知识库文档是否正确上传和分片、编排流程中的知识库节点是否正确配置并指向了您的知识库、以及应用是否已成功部署到生产环境并且设备已重启。

## 第七章：聆思流程编排中的Node-RED核心概念解读 (概念映射)

对于熟悉 [Node-RED](https://nodered.org/) ([Node-RED Cookbook - 触发流程](https://cookbook.nodered.org/basic/trigger-a-flow) 等资源也很有用) 的开发者来说，将已有的知识映射到聆思平台 (LSPlatform) 的端云一体化流程编排上，可以更快地理解和上手。虽然具体节点名称和实现可能不同，但核心的事件驱动、流式处理思想是相通的。本章旨在从概念层面建立这种映射。

### 7.1. 输入/触发节点 (Input/Inject Nodes)

在任何流程驱动的系统中，流程的启动都需要一个“起点”或“触发器”。

*   **聆思平台 (LSPlatform) 中的体现：**
    *   **设备交互触发：** 在端云一体化场景中，最常见的触发方式是用户与聆思大模型开发套件的物理交互。例如：
        *   **语音指令：** 用户通过语音唤醒设备或按下录音键后发出的语音指令，会作为流程的初始输入。在第一个教程（基于`ONjKxNdxZ`文档）中我们看到的“对话入口”节点，以及在[《接入云端可编排应用》](https://docs2.listenai.com/x/RtdU8yrbO)文档所描述的模板流程中，设备端的语音识别结果（ASR文本）就是驱动云端流程的关键输入。
        *   **按键事件：** 开发套件上的物理按键（如K3键）被按下，可以触发特定的流程。
        *   **传感器数据：** 如果连接了外部传感器，传感器数据的变化（例如，检测到有人靠近、温度超过阈值）也可以配置为流程的触发条件（尽管具体实现可能需要特定的设备端节点或逻辑）。
    *   **云端入口节点：** 某些流程可能由云端事件触发，例如接收到一个特定的API调用或定时任务。

*   **Node-RED 中的概念对应：**
    *   **`inject` 节点 ([Node-RED Inject Node文档](https://nodered.org/docs/user-guide/nodes#inject))：** 这是Node-RED中最直接的触发方式，可以手动触发，也可以配置为定时或在启动时自动触发，向流程注入一个消息。
    *   **输入型节点：** 例如 `mqtt in`（订阅MQTT消息）、`http in`（创建HTTP端点接收请求）、`websocket in` 等。这些节点会监听外部事件，并将接收到的数据作为消息注入流程。
    *   聆思设备上的用户交互（语音、按键）在概念上类似于这些输入型节点，它们是流程的起点，将外部世界的事件转换成内部流程可以处理的“消息”。

### 7.2. 功能/处理节点 (Function/Processing Nodes)

一旦流程被触发，消息就会在节点间流动，并由各种功能/处理节点进行加工。

*   **聆思平台 (LSPlatform) 中的体现：**
    *   **AI服务节点：** 这是LSPlatform的核心优势。平台提供了封装好的、强大的AI服务节点，例如：
        *   “大模型”节点：用于调用大语言模型进行对话、问答、文本生成等。
        *   “文生图”节点：根据文本描述生成图片。
        *   “图生文”/“图片理解”节点：分析图片内容并生成文字描述。
        *   “知识点检索”节点：在指定的知识库中查找与输入相关的条目。
        *   “语音合成 (TTS)”节点：将文本转换为语音输出。
        这些节点将复杂的AI调用封装起来，用户只需关注其输入和输出即可。
    *   **逻辑与控制节点：** 平台通常也会提供用于控制流程走向的节点，如条件判断、数据转换、意图识别（本身也是一种AI处理）等。

*   **Node-RED 中的概念对应：**
    *   **`function` 节点 ([Node-RED Function Node文档](https://nodered.org/docs/user-guide/nodes#function))：** 这是Node-RED中最灵活的处理节点，允许用户使用JavaScript编写自定义的逻辑来处理消息。
    *   **核心处理节点：** Node-RED内置了许多用于数据处理的节点，例如：
        *   `switch` 节点：根据消息内容将消息路由到不同的分支。
        *   `change` 节点：修改消息的属性。
        *   `template` 节点：根据模板生成文本输出。
    *   聆思平台的AI服务节点在概念上可以看作是高度特化和封装的“超级”处理节点。它们背后可能调用了复杂的模型和API，但对用户屏蔽了这些复杂性。

*   **关于自定义代码节点：**
    *   在Node-RED中，`function`节点是实现高度自定义逻辑的主要方式。
    *   在聆思平台的端云一体化流程编排中，主要的自定义方式是通过选择和配置预设的节点以及调整它们之间的连接来实现。虽然平台可能不直接提供一个让用户随意编写如JavaScript等前端脚本的“通用function节点”，但其丰富的AI节点和逻辑控制节点已经能够满足绝大多数应用场景的需求。更深度的定制可能涉及到在设备端（而非云端编排流程中）进行固件级别的开发，或者通过API与其他服务集成。

### 7.3. 调试节点 (Debug Nodes)

在开发和调试流程时，能够查看节点之间传递的消息内容至关重要。

*   **聆思平台 (LSPlatform) 中的体现：**
    *   **辅助栏日志/节点信息：** 在第一个教程（基于`ONjKxNdxZ`文档）中，我们了解到“在线编排界面”包含“辅助栏”，它可以“显示节点信息、应用日志、平台帮助以及可在此配置节点”。这部分功能，特别是“应用日志”和“节点信息”，可以帮助开发者追踪流程执行状态和消息内容。
    *   **AIFlow调试技巧：** 聆思的[《AIFlow调试技巧》文档](https://docs2.listenai.com/x/4-XTiG_ks)（虽然具体内容未在此提供，但其存在表明平台重视调试能力）可能会介绍更高级的调试节点或方法，例如如何检查特定节点输入输出的详细数据，或如何在流程中插入专门的“日志节点”或“调试探针”。

*   **Node-RED 中的概念对应：**
    *   **`debug` 节点 ([Node-RED Debug Node文档](https://nodered.org/docs/user-guide/nodes#debug))：** 这是Node-RED中用于调试的核心节点。将其连接到任何节点的输出端口，就可以在编辑器的“调试”侧边栏中看到流经该点的消息对象的完整内容。
    *   聆思平台的“辅助栏”中的日志功能，以及可能的专用调试节点（如“AIFlow调试技巧”文档所介绍的），在概念上承担了与Node-RED `debug`节点类似的角色，即提供对流程内部状态和数据流的可见性。

### 7.4. 输出节点 (Output Nodes)

流程处理完毕后，结果通常需要被发送到某个地方或执行某个动作。

*   **聆思平台 (LSPlatform) 中的体现：**
    *   **设备端动作：** 这是端云一体化流程的关键闭环。云端处理的结果最终会通过特定的节点（例如，TTS节点将文本合成为语音流并传给设备播放，或显示节点将图片/文本显示在设备屏幕上）反馈到聆思大模型开发套件上执行。
    *   **调用外部服务：** 流程的结果也可能通过API调用等方式发送给第三方服务。
    *   **“流程结束”节点：** 在第一个教程（基于`ONjKxNdxZ`文档）的简单编排中，“流程结束”节点标志着一次交互的完成，其最终结果（如大模型的回复）会隐式地通过与“对话入口”节点的联动返回给用户。

*   **Node-RED 中的概念对应：**
    *   **输出型节点：** 例如 `mqtt out`（发布MQTT消息）、`http response`（响应进入的HTTP请求）、`websocket out`，以及各种与其他物理设备（如GPIO、串口设备）或在线服务（如Twitter、数据库）交互的节点。
    *   聆思平台中那些将处理结果（如TTS语音、屏幕显示内容）发送回开发套件的节点，在概念上等同于Node-RED中与硬件或外部接口交互的输出节点。它们将流程内部的数据转换成外部世界可以感知或接收的信号/动作。

通过以上概念映射，熟悉Node-RED的开发者可以更快地将已有的经验和理解迁移到聆思平台的端云一体化流程编排中，从而更高效地利用平台提供的强大功能构建智能应用。尽管工具的具体实现有所不同，但其核心的流式编程和事件驱动思想是一致的。

## 第八章：问题排查与后续学习

在您探索和实践聆思平台 (LSPlatform) 端云一体化流程编排的过程中，可能会遇到一些问题。本章旨在提供一些常见问题的排查思路，并为您指引后续学习的方向。

### 8.1. 常见问题与排查思路

1.  **设备无法成功连接到LSPlatform / 应用无响应：**
    *   **检查网络连接：** 确保您的聆思大模型开发套件已按照[《语音交互+识图 功能体验》文档中的“配置网络”部分](https://docs2.listenai.com/x/2V18-j2v2#配置网络)正确连接到2.4G Wi-Fi网络，并且网络通畅。
    *   **核对产品凭证：** 仔细检查设备端写入的“产品ID (Product ID)”和“产品密钥 (Secret ID)”是否与您在LSPlatform上创建的“产品”完全一致。任何微小的输入错误都可能导致认证失败。参考本教程 **5.5. 设备端写入产品凭证** 部分。
    *   **检查设备白名单：** 确认设备的唯一ID (Chip ID) 已被正确添加到目标“产品”的设备白名单中。参考本教程 **5.4. 添加设备到白名单** 部分。
    *   **应用部署状态：** 确保您在LSPlatform上的应用已经成功“部署生产”。参考本教程 **6.4. 部署变更** 部分。
    *   **设备重启：** 在云端配置或应用部署发生变更后，是否已按要求重启开发套件？

2.  **编排的流程行为不符合预期：**
    *   **检查节点配置：** 逐个检查流程中关键节点的配置参数是否正确。例如，大模型节点的模型选择、知识库节点的知识库选择、TTS节点的发音人选择等。双击节点通常可以打开其配置面板。
    *   **分析消息流：**
        *   利用LSPlatform“在线编排”界面右侧的“辅助栏”中的“应用日志”或“节点信息”功能，观察消息在节点间的传递过程和内容。
        *   更详细的调试方法，可以参考聆思官方提供的[《AIFlow调试技巧》文档](https://docs2.listenai.com/x/4-XTiG_ks)，学习如何使用平台提供的调试工具或节点来诊断问题。
    *   **检查连接逻辑：** 仔细核对节点之间的连接线是否准确地反映了您期望的业务逻辑顺序和数据流向。错误的连接可能导致流程在非预期的分支执行或中断。
    *   **分段测试：** 如果流程较为复杂，可以尝试暂时简化流程，或在关键节点后添加调试输出，分段验证各部分是否正常工作，逐步定位问题所在。

3.  **知识库问答不返回正确答案或不生效：**
    *   **知识库内容核查：** 确认您提出的问题确实与上传到知识库的文档内容相关，并且文档内容表述清晰、准确。
    *   **分片策略检查：** 虽然通常使用默认的“智能分片”，但如果文档结构特殊，可能需要审视分片是否合理。
    *   **知识库选择：** 在“知识点检索”节点的配置中，再次确认是否选择了正确的、已上传文档的知识库。
    *   **API Key权限：** 如本教程 **6.2. 在LSPlatform创建知识库** 中强调的，确保为知识库服务创建并启用了有效的API Key。
    *   **流程逻辑：** 检查知识库检索节点在整个流程中的位置是否合理。例如，是否有可能用户的提问在到达知识库检索节点之前，就被其他意图识别或逻辑分支处理了？

4.  **语音识别不准确 / TTS发音不自然：**
    *   **环境因素：** 检查设备所处的物理环境，减少噪音干扰，确保麦克风拾音清晰。
    *   **TTS节点配置：** 尝试更换不同的发音人或调整语速、语调等参数。
    *   **ASR与TTS模型：** 某些情况下，可能是所选ASR（语音识别）或TTS（语音合成）模型本身的限制，可以关注平台是否提供了不同版本或更高级的模型选项。

### 8.2. 后续学习资源

掌握了本教程的内容后，您已经具备了进行聆思平台端云一体化流程编排的基础。为了进一步提升您的开发技能和应用复杂度，建议您：

1.  **深入阅读聆思官方文档：**
    *   [《LSPlatform 开始使用》](https://docs2.listenai.com/x/WqRCmE3dA)：全面了解LSPlatform的各项功能和操作。
    *   [《通过编排新增AIUI技能调用》](https://docs2.listenai.com/x/8iHmqHiBk)：学习如何在现有的大模型应用基础上，通过流程编排集成和调用AIUI平台提供的其他AI技能。
    *   [《端云交互链路协议》](https://docs2.listenai.com/x/I7T_gajht)：如果您需要更深入地理解设备与云端之间的通信机制和协议细节，这份文档会很有帮助。
    *   [《AIFlow调试技巧》](https://docs2.listenai.com/x/4-XTiG_ks)：深入学习平台提供的调试工具和方法，提高问题排查效率。
    *   **教程视频：** 观看[《教程视频——云端节点编排》](https://b23.tv/gPY1HBU)，通过视频演示更直观地学习节点编排的操作。
    *   **其他相关文档：** 留意`RtdU8yrbO`（即[《接入云端可编排应用》](https://docs2.listenai.com/x/RtdU8yrbO)）文档末尾的“相关内容与链接”部分，通常会汇总更多有价值的参考资料。

2.  **大胆实验与探索：**
    *   **尝试不同的节点：** 在LSPlatform的“在线编排”界面，从“节点栏”中拖拽出您在本教程中未使用过的节点，查看其配置项，尝试理解其功能，并将其集成到您的测试流程中。
    *   **构建更复杂的流程：** 挑战自己，设计并实现具有多分支条件判断、并行处理（如果平台支持）、多次调用AI服务、或与多个外部系统交互的复杂应用流程。
    *   **优化交互体验：** 思考如何通过流程编排来提升用户与设备交互的自然度、流畅性和智能化程度。

3.  **借鉴Node-RED等通用流程编程思想 (可选)：**
    *   如果您希望进一步加深对流程化、事件驱动编程范式的理解，可以回顾或学习更多关于[Node-RED的官方文档](https://nodered.org/docs/)或相关教程（如[Node RED Programming Guide](https://noderedguide.com/)）。这些通用的概念和技巧有时能为您在LSPlatform上设计流程提供新的思路和灵感。

聆思平台作为一个持续发展的技术平台，其功能和文档也会不断更新和完善。建议您保持关注聆思官方的最新动态和开发者社区，以便获取最新的信息和支持。祝您在端云一体化应用开发的道路上不断进步，创造出更多富有创意的智能应用！

## 第九章：总结与展望

恭喜您完成了本次聆思 (ListenAI) 端云一体化流程编排上手教程！通过本教程的学习和实践，您应该已经对如何利用聆思大模型开发套件和[聆思平台 (LSPlatform)](https://platform.listenai.com/) 构建和自定义智能应用有了初步但坚实的理解。

### 9.1. 本教程回顾

在本教程中，我们一起走过了以下关键步骤：

1.  **核心概念理解：** 了解了聆思端云一体化流程编排的基本思想，以及其与Node-RED等可视化编程工具在节点、流程、消息等核心概念上的相似之处。
2.  **环境准备与设置：** 完成了必要的硬件（聆思大模型开发套件）、软件（“语音交互+识图”应用）、网络配置以及LSPlatform账户的准备工作。
3.  **创建与关联：**
    *   学习了如何在LSPlatform上基于“大模型套件语音交互与识图模板”（主要参考[《接入云端可编排应用》](https://docs2.listenai.com/x/RtdU8yrbO)文档）创建云端应用。
    *   掌握了创建“产品”、将云端应用配置到“产品”、获取设备ID并将其添加到“产品”白名单，以及最终在设备端写入产品凭证，从而将物理设备与云端应用成功关联起来的全过程。
4.  **理解与自定义流程：**
    *   初步探索了模板应用预设的端云一体化流程。
    *   通过一个实际示例——为应用集成自定义知识库——学习了如何在LSPlatform上创建知识库、上传文档，并修改现有编排流程中的“知识点检索”节点以调用新知识库。
5.  **部署与测试：** 了解了如何将修改后的应用部署到生产环境，并指导了如何通过与开发套件的实际交互来测试自定义后的知识库问答功能。

### 9.2. 端云一体化的力量与灵活性

通过本教程，希望您能体会到将强大的硬件（如聆思大模型开发套件）与灵活的云端可视化流程编排（通过LSPlatform）相结合所带来的巨大潜力。这种模式：

*   **简化了复杂应用的开发：** 将复杂的AI能力（如大语言模型、知识库、TTS、视觉识别等）封装为易于使用的节点，让开发者可以专注于业务逻辑和用户体验创新。
*   **加速了产品原型验证与迭代：** 可视化的流程调整和快速的云端部署，使得验证新想法、迭代产品功能变得更加高效。
*   **赋予了硬件更强的智能：** 使得硬件设备不再是孤立的执行单元，而是能够动态调用和集成不断更新的云端AI服务，从而实现更高级、更灵活的智能化。

### 9.3. 展望与后续探索

您现在已经掌握了聆思端云一体化流程编排的基础。这为您打开了通往构建更复杂、更个性化智能硬件应用的大门。我们强烈鼓励您：

*   **继续探索LSPlatform：** 深入研究平台提供的其他类型节点、高级功能以及更复杂的编排技巧。查阅我们在第八章中提到的[后续学习资源](new_tutorial_section_8.md)，特别是聆思官方的各类文档和教程。
*   **实践您的创意：** 将所学知识应用到您自己的项目中。思考您希望您的智能设备实现哪些独特的功能，然后尝试通过LSPlatform的流程编排来实现它们。无论是智能家居控制、特定行业的AI助手，还是创新的多模态交互应用，都有广阔的发挥空间。
*   **保持学习与关注：** AI和物联网技术日新月异，聆思平台也在不断发展。保持对新技术、新功能的好奇心和学习热情，将使您始终处于技术创新的前沿。

感谢您跟随本教程的学习。我们期待看到您利用聆思的端云一体化能力，创造出更多令人兴奋的智能产品和解决方案！祝您在AIoT的探索之旅中一切顺利！
