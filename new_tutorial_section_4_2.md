### 4.2. 理解默认编排流程

创建完应用后，下一步是进入其编排界面，熟悉模板预设的流程和节点。这将帮助我们理解端云一体化应用是如何通过可视化方式构建和运作的。

1.  **打开新创建的应用并进入编排界面:**
    *   返回到聆思平台 (LSPlatform) 的应用列表或应用管理界面。
    *   找到您在上一节 (4.1) 中创建的应用，并点击它。
    *   在应用详情页面，点击“编排应用”按钮。这一步骤在聆思官方的[“创建新应用”文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)中也有图示说明（通常在模板创建成功后，下一步即是进入编排界面）。
        *(聆思文档此处会有一张图片，展示了应用详情页以及“编排应用”按钮的位置。请参考原始文档截图，标记为：“进入应用编排界面示意图”)*

2.  **探索预设的编排流程:**
    点击“编排应用”后，您将进入可视化应用编排界面。您会看到一个已经由许多节点和连接线组成的流程图。这就是“大模型套件语音交互与识图模板”预设的端云一体化业务流程。您可以对照[“创建新应用”文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)中展示的示例工程节点图和业务流程图来理解这个默认编排。
    *(聆思文档[“创建新应用”](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)章节此处会有一张图片，展示了模板应用默认的节点编排图。请参考原始文档截图，标记为：“模板应用默认编排流程图”)*

    根据[该文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)的描述，这个预设的示例工程实现了一个相当智能的多模态交互流程，主要包括以下功能：
    *   **图生文 (Image-to-Text):** 理解用户拍摄的图片内容。
    *   **文生图 (Text-to-Image):** 根据用户语音描述生成图片。
    *   **知识库问答 (Knowledge Base Q&A):** 基于您提供的知识库回答特定领域问题。
    *   **兜底闲聊 (Fallback Chat):** 在无法匹配特定意图或知识库内容时，进行通用聊天。

    **流程逻辑解析 (依据[官方文档](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)流程解析部分)：**
    该模板的业务流程大致如下：
    1.  **优先处理连续对话中的图生文：** 系统首先会检查最近（例如3轮内）的对话历史，判断用户是否正在进行一个关于图片的连续对话（例如，用户上传了一张图，然后继续追问关于这张图的问题）。如果是，则继续调用大模型进行图片理解，并将结果通过流式TTS（语音合成）和流式文本下发给设备。
    2.  **意图识别（是否“画画”）：** 如果不是连续的图生文场景，系统会利用大模型的意图识别能力，判断用户的输入是否包含“画画”或类似的意图。
    3.  **执行文生图：** 如果用户的意图被识别为“画画”，流程将转向文生图逻辑。这包括调用相应的AI模型根据用户的文本描述生成图片，并将生成的图片显示在设备屏幕上，同时可能伴有TTS语音反馈。
    4.  **知识库检索：** 如果用户的意图不是“画画”，系统会尝试在预设的知识库中检索与用户提问相关的内容。如果找到了匹配的知识点，则调用大模型围绕这些知识点组织答案，并通过流式TTS和文本下发。
    5.  **兜底闲聊：** 如果用户的意图既不是“画画”，在知识库中也没有找到相关答案，那么流程会进入兜底闲聊模式。系统会调用大模型进行通用的、开放式的对话回复，并通过流式TTS和文本下发。

3.  **关键节点解读 (初探):**
    在流程图中，您会看到多种不同功能的节点。虽然我们将在后续章节详细介绍如何配置它们，但这里可以初步认识几个核心的聆思平台特有节点（具体名称和图标请参照您界面中的实际显示，也可对照[官方文档中的流程图](https://docs2.listenai.com/x/RtdU8yrbO#创建新应用)进行识别）：
    *   **图片理解/视觉模型节点:** 用于处理和分析从设备端上传的图像数据，实现“图生文”功能。
    *   **文本生成/大语言模型节点:** 核心的LLM调用节点，用于意图识别、组织回答、生成对话内容以及“文生图”中的文本处理。
    *   **知识点检索节点:** 连接到您在LSPlatform上创建的知识库，根据输入文本检索相关知识条目。
    *   **TTS流式合成节点:** 将文本（例如大模型的回复、知识库答案）实时合成为语音流，并发送到设备端播放。
    *   **设备输入/输出节点:** 代表与大模型开发套件硬件的交互，例如接收语音指令、获取摄像头图像、在屏幕上显示文本或图片、播放音频等。

通过对这个默认流程的初步探索，您可以大致了解一个端云一体化应用是如何通过不同功能节点的组合与连接，来实现复杂的多模态AI交互逻辑的。在接下来的步骤中，我们将学习如何修改和定制这个流程。
