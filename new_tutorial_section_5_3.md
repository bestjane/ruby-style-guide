### 5.3. 获取设备ID (Chip ID)

每个聆思大模型开发套件都有一个唯一的设备ID (也称为 Chip ID)。这个ID是设备在聆思平台 (LSPlatform) 上进行身份认证和实现端云交互的关键凭证。在将设备添加到您创建的“产品”的设备白名单之前，我们必须先获取到这个ID。

聆思官方文档提供了以下几种获取设备ID的方法：

#### 方法一：从设备设置中查看

这种方法直接在开发套件的屏幕上操作，最为直观。

1.  **确保设备运行“语音交互+识图”应用：** 您的开发套件应已启动并正在运行“大模型语音交互+识图”这个示例应用。
2.  **进入应用信息查看页：**
    *   在应用的主界面（待机页面），从屏幕顶部向下滑动，调出下拉菜单。
    *   点击菜单中的“设置”图标（通常是一个齿轮形状的图标）。
    *   在设置菜单中，找到并点击“应用”选项。
    *   进入“应用信息查看页”后，您就可以看到当前设备的ID信息。
        *(聆思文档此处会有一张图片，展示了在设备屏幕上通过 设置 → 应用 查看设备ID的完整路径和最终显示ID的界面。请参考原始文档截图，标记为：“设备设置中查看ID示意图”)*

#### 方法二：通过串口指令读取

如果您习惯使用命令行或者设备屏幕操作不便，可以通过串口连接来获取设备ID。

1.  **连接DAP_USB接口：** 使用USB数据线将开发套件的DAP_USB接口连接到您的电脑。
2.  **打开串口终端工具：**
    *   您可以使用聆思官方提供的“[在线串口终端](https://tool.listenai.com/serial-term/)”。
    *   或者使用您电脑上安装的其他串口调试工具（如PuTTY, Tera Term等）。
    *   确保选择了正确的串口号，波特率通常默认为115200。
3.  **发送获取ID指令：**
    *   在串口终端中，输入以下指令并发送（回车）：
        ```
        get chip_id
        ```
    *   发送指令后，设备ID将会显示在终端的返回信息中。
        *(聆思文档此处会有一张图片，展示了通过串口终端发送 `get chip_id` 指令并成功获取到设备ID的界面。请参考原始文档截图，标记为：“串口指令获取ID示意图”)*

#### 方法三：使用 `cskburn desktop` 烧录工具读取

`cskburn desktop` 是聆思官方推出的桌面烧录工具，除了固件烧录功能外，也可以用来获取设备信息。

1.  **下载并安装工具：**
    *   如果尚未安装，请先下载并安装 [`cskburn desktop` 烧录工具](https://docs2.listenai.com/x/oo2_KzYFd)。 (请注意：此链接指向的是包含`cskburn desktop`工具的文档页面，实际下载链接可能在该页面内。)
2.  **运行工具并连接设备：**
    *   确保您的开发套件通过USB（通常是DAP_USB口）连接到电脑。
    *   双击 `cskburn desktop` 工具的图标运行软件。
3.  **获取设备信息：**
    *   在 `cskburn desktop` 工具界面中，首先从串口下拉框中选择您的开发套件连接后被电脑识别到的串口编号。
    *   然后，点击界面上的“获取信息”按钮。
    *   工具执行后，会在相应的区域显示出包括设备ID在内的设备信息。
        *(聆思文档此处会有一张图片，展示了 `cskburn desktop` 工具的界面，包括选择串口、点击“获取信息”按钮以及显示设备ID的区域。请参考原始文档截图，标记为：“cskburn工具获取ID示意图”)*

请选择以上任意一种方法获取您的设备ID，并妥善记录下来。在接下来的步骤中，我们会用到这个ID来完成设备在云端的注册。
